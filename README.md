- git checkout master
- git pull origin master
- git checkout -b new-feature

- ** make changes **
- ** add change **
- ** commit changes **

- git checkout master
- git pull origin master

- git checkout new-feature
- git rebase master
- git push origin new-feature
